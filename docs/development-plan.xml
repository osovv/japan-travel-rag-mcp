<DevelopmentPlan VERSION="1.6.0">
  <Context>
    <purpose>Deliver a multi-country travel platform with single MCP endpoint, shareable onboarding, curated-site retrieval, and per-country data routing: portal onboarding with platform branding, fixed MCP tool surface (4 proxied + 3 local) with `country_code` parameter, worker-based crawl/index pipeline, and attributed evidence retrieval for travel agents across multiple destinations.</purpose>
    <scope>Transform single-destination Japan server into multi-country platform with `country_code` tool parameter routing; keep existing proxied tg-chat tools (`search_messages`, `get_message_context`, `get_related_messages`, `list_sources`) and local tools (`get_site_sources`, `search_sites`, `get_page_chunk`); add `country_settings` table for per-country runtime config; add `country_code` columns to `site_sources` and `usage_counters`; keep `/admin/*` root-token protected; keep portal-first onboarding on `/` and `/portal/*` with platform branding and destination list; maintain layered per-source cleanup pipeline and quality gating.</scope>
    <constraints>
      <constraint-1>Public MCP surface remains exactly seven tools: `search_messages`, `get_message_context`, `get_related_messages`, `list_sources` (proxied to upstream), and `get_site_sources`, `search_sites`, `get_page_chunk` (local).</constraint-1>
      <constraint-2>`search_context`, `validate_google_map_link`, and `verify_tabelog` are removed from active and deferred implementation plans.</constraint-2>
      <constraint-3>Landing remains intentionally simple and redirects users into user portal; invite link is not required in this phase.</constraint-3>
      <constraint-4>User portal must support self-serve onboarding through Logto social OAuth only (no email/password auth forms) and avoid manual operator permission grants.</constraint-4>
      <constraint-5>Portal onboarding must culminate in actionable MCP connection instructions for external AI agents and deterministic OAuth callback handling.</constraint-5>
      <constraint-6>FastMCP OAuth Proxy is enabled only for `/mcp` transport surface.</constraint-6>
      <constraint-7>`/admin/*` is protected only by `ROOT_AUTH_TOKEN` login/session policy.</constraint-7>
      <constraint-8>Portal session auth on `/portal/*` is isolated from both `/mcp` OAuth and `/admin/*` root session models.</constraint-8>
      <constraint-9>Curated-source ingestion/indexing is active scope and runs only in worker runtime with crawl-only ingestion policy.</constraint-9>
      <constraint-10>Billing settings are deferred and represented as future extension points in portal architecture only.</constraint-10>
      <constraint-11>Usage tracking failures must never block MCP tool execution; tracking is fire-and-forget with warning logs on failure.</constraint-11>
      <constraint-12>User identity for usage tracking is extracted from MCP OAuth session JWT `sub` claim; the same Logto `sub` links MCP and portal users.</constraint-12>
      <constraint-13>Unified cross-source search tool is intentionally out of scope; caller must use `search_messages` and `search_sites` explicitly.</constraint-13>
      <constraint-14>`get_page_chunk` returns bounded excerpts only; full-page verbatim dump is not exposed.</constraint-14>
      <constraint-15>All Spider and Voyage calls must go through unified proxy (`PROXY_BASE_URL` + `PROXY_SECRET`) with service keys.</constraint-15>
      <constraint-16>Runtime is split into dedicated API and Worker entrypoints; no single-binary `APP_ROLE` toggle.</constraint-16>
      <constraint-17>Curated crawling in v1 is crawl-only (scheduled + internal targeted recrawl), with no public manual-refresh endpoint.</constraint-17>
      <constraint-18>Parser cleanup is deterministic and rule-based, runs before chunking/embedding, and uses layered pipeline (global → source adapter → quality gate); cleanup results are discriminated union (accepted vs skipped) with structured skip reasons.</constraint-18>
      <constraint-19>Layer B source adapters must cover all active curated `source_id` values from `site_sources`; after `wrenjapan`, rollout continues for insidekyoto, trulytokyo, kansai_odyssey, invisible_tourist, japan_unravelled, japan_guide, reddit_japantravel, navitime, jorudan, jreast, and smart_ex.</constraint-19>
      <constraint-20>Single MCP endpoint `/mcp` serves all countries; tools that touch country-specific data require `country_code` parameter (ISO 3166-1 alpha-2); `country_code` enum is built dynamically from `country_settings` rows with `status='active'` at startup.</constraint-20>
      <constraint-21>`country_settings` table is the single source of truth for per-country runtime config; `TG_CHAT_RAG_CHAT_IDS` env var is deprecated and replaced by `country_settings.settings.tg_chat_ids`.</constraint-21>
      <constraint-22>Adding a new country requires only DB inserts (`country_settings` + `site_sources` rows) and server restart; no code changes needed.</constraint-22>
      <constraint-23>Tools without country-specific data (`get_message_context`, `get_related_messages`, `get_page_chunk`) do NOT get `country_code` parameter — they use entity UIDs that are already scoped.</constraint-23>
      <constraint-24>Portal branding uses generic platform name from `PLATFORM_NAME` env var; portal shows destination list from `country_settings` with status-appropriate display (active = shown, coming_soon = teaser, draft/maintenance = hidden or notice).</constraint-24>
    </constraints>
  </Context>

  <Modules>
    <M-CONFIG NAME="Config" TYPE="UTILITY" LAYER="0" ORDER="1">
      <contract>
        <purpose>Load and validate runtime configuration for MCP proxy auth/routes, admin auth, portal auth/session, upstream integrations, database connection, and platform branding.</purpose>
        <inputs>
          <param-1 name="env" type="NodeJS.ProcessEnv" />
        </inputs>
        <outputs>
          <param-1 name="AppConfig" type="object" />
        </outputs>
        <errors>
          <error-CONFIG_VALIDATION_ERROR code="CONFIG_VALIDATION_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-loadConfig PURPOSE="Validate env and return typed AppConfig." />
        <type-AppConfig PURPOSE="Typed runtime config for MCP OAuth, portal session, admin auth, upstream clients, database URL, and PLATFORM_NAME." />
      </interface>
      <depends>none</depends>
      <notes>
        <note-1>Required env includes existing MCP/admin/logto values plus `PORTAL_SESSION_SECRET`, `LOGTO_M2M_APP_ID`, `LOGTO_M2M_APP_SECRET`, `LOGTO_MCP_USER_ROLE_ID`, `DATABASE_URL`, `VOYAGE_API_KEY`, `SPIDER_API_KEY`, `PROXY_BASE_URL`, `PROXY_SECRET`, and `PLATFORM_NAME`. `TG_CHAT_RAG_CHAT_IDS` is deprecated (optional, backwards compat during migration).</note-1>
      </notes>
    </M-CONFIG>

    <M-LOGGER NAME="Logger" TYPE="UTILITY" LAYER="0" ORDER="2">
      <contract>
        <purpose>Provide structured logs for startup, auth decisions, onboarding routes, tool execution, and upstream failures.</purpose>
        <inputs>
          <param-1 name="config" type="AppConfig" />
        </inputs>
        <outputs>
          <param-1 name="Logger" type="object" />
        </outputs>
        <errors>
          <error-LOGGER_INIT_ERROR code="LOGGER_INIT_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-createLogger PURPOSE="Create module-scoped logger with semantic block labels." />
      </interface>
      <depends>M-CONFIG</depends>
    </M-LOGGER>

    <M-TOOLS-CONTRACTS NAME="ToolContracts" TYPE="UTILITY" LAYER="1" ORDER="1">
      <contract>
        <purpose>Define deterministic Zod schemas and tool contracts for proxied tg-chat tools and local curated-site tools, with dynamic `country_code` enum for country-scoped tools.</purpose>
        <inputs>
          <param-1 name="rawArgs" type="unknown" />
          <param-2 name="toolName" type="string" />
          <param-3 name="activeCountryCodes" type="string[]" />
        </inputs>
        <outputs>
          <param-1 name="validatedArgs" type="object" />
          <param-2 name="allowlist" type="readonly string[]" />
          <param-3 name="toolDescriptions" type="record" />
          <param-4 name="toolSchemas" type="ToolSchemas" />
        </outputs>
        <errors>
          <error-SCHEMA_VALIDATION_ERROR code="SCHEMA_VALIDATION_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-SearchMessagesInputPublic PURPOSE="Public input schema for search_messages with country_code and forbidden filters.chat_ids path." />
        <export-GetMessageContextInput PURPOSE="Input schema for get_message_context." />
        <export-GetRelatedMessagesInput PURPOSE="Input schema for get_related_messages." />
        <export-ListSourcesInput PURPOSE="Input schema for list_sources with country_code." />
        <export-SearchSitesInput PURPOSE="Input schema for search_sites (query, country_code, top_k, source_ids)." />
        <export-GetPageChunkInput PURPOSE="Input schema for get_page_chunk (chunk_id, include_neighbors)." />
        <export-PROXIED_TOOL_NAMES PURPOSE="Stable allowlist for tool registration and proxy dispatch." />
        <export-TOOL_INPUT_JSON_SCHEMAS PURPOSE="Tool descriptions used by FastMCP addTool metadata." />
        <export-buildToolSchemas PURPOSE="Build tool schemas with dynamic country_code enum from active country codes at startup." />
        <type-ToolSchemas PURPOSE="Record of per-tool JSON schemas with injected country_code enum." />
      </interface>
      <depends>M-LOGGER</depends>
    </M-TOOLS-CONTRACTS>

    <M-TG-CHAT-RAG-CLIENT NAME="TgChatRagClient" TYPE="INTEGRATION" LAYER="2" ORDER="1">
      <contract>
        <purpose>Call tg-chat-rag methods API and normalize protocol/timeouts/errors for proxy handlers.</purpose>
        <inputs>
          <param-1 name="toolName" type="string" />
          <param-2 name="payload" type="object" />
        </inputs>
        <outputs>
          <param-1 name="upstreamResponse" type="object" />
        </outputs>
        <errors>
          <error-UPSTREAM_HTTP_ERROR code="UPSTREAM_HTTP_ERROR" />
          <error-UPSTREAM_TIMEOUT code="UPSTREAM_TIMEOUT" />
          <error-UPSTREAM_PROTOCOL_ERROR code="UPSTREAM_PROTOCOL_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-callMethod PURPOSE="POST /api/v1/methods/&lt;tool_name&gt; with service bearer token." />
      </interface>
      <depends>M-CONFIG, M-LOGGER</depends>
    </M-TG-CHAT-RAG-CLIENT>

    <M-AUTH-CONSENT-PATCH NAME="ConsentPatch" TYPE="UI_COMPONENT" LAYER="2" ORDER="2">
      <contract>
        <purpose>Replace FastMCP built-in consent screen with portal-styled HTML via monkey-patching OAuthProxy.consentManager.</purpose>
        <inputs>
          <param-1 name="oauthProxy" type="OAuthProxy" />
        </inputs>
        <outputs>
          <param-1 name="void" type="void" />
        </outputs>
        <errors>
          <error-CONSENT_PATCH_ERROR code="CONSENT_PATCH_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-patchOAuthProxyConsent PURPOSE="Monkey-patch OAuthProxy.consentManager.generateConsentScreen with portal-styled HTML renderer." />
        <export-generatePortalConsentScreen PURPOSE="Render portal-styled consent HTML with scope labels, XSS escaping, and form POST to /oauth/consent." />
      </interface>
      <depends>M-AUTH-PROXY</depends>
    </M-AUTH-CONSENT-PATCH>

    <M-AUTH-PROXY NAME="FastMcpOauthProxy" TYPE="INTEGRATION" LAYER="2" ORDER="3">
      <contract>
        <purpose>Create and configure FastMCP OAuthProxy bound to Logto OIDC endpoints for `/mcp` authentication.</purpose>
        <inputs>
          <param-1 name="config" type="AppConfig" />
        </inputs>
        <outputs>
          <param-1 name="oauthProxy" type="OAuthProxy" />
          <param-2 name="authorizationServerMetadata" type="object" />
        </outputs>
        <errors>
          <error-OAUTH_PROXY_INIT_ERROR code="OAUTH_PROXY_INIT_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-createOauthProxy PURPOSE="Instantiate OAuthProxy with Logto auth/token endpoints, configured scopes, and portal consent patch." />
      </interface>
      <depends>M-CONFIG, M-LOGGER, M-AUTH-CONSENT-PATCH</depends>
    </M-AUTH-PROXY>

    <M-PORTAL-IDENTITY NAME="PortalIdentityClient" TYPE="INTEGRATION" LAYER="2" ORDER="3">
      <contract>
        <purpose>Wrap identity-provider operations needed by portal social OAuth flows: start URL construction, callback token exchange, identity resolution, and role/permission updates for tester access.</purpose>
        <inputs>
          <param-1 name="provider" type="string" />
          <param-2 name="intent" type="register|login" />
          <param-3 name="callbackParams" type="object" />
          <param-4 name="userId" type="string" />
        </inputs>
        <outputs>
          <param-1 name="oauthStartUrl" type="string" />
          <param-2 name="identityResult" type="object" />
        </outputs>
        <errors>
          <error-PORTAL_IDENTITY_ERROR code="PORTAL_IDENTITY_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-buildPortalOauthStartUrl PURPOSE="Build Logto authorization URL with social direct-sign-in parameters and portal callback target." />
        <export-handlePortalOauthCallback PURPOSE="Exchange callback authorization code and resolve portal identity profile." />
        <export-ensureMcpAccessRole PURPOSE="Ensure tester has required MCP access role/policy without manual admin action." />
      </interface>
      <depends>M-CONFIG, M-LOGGER</depends>
    </M-PORTAL-IDENTITY>

    <M-TOOL-PROXY NAME="ToolProxyService" TYPE="CORE_LOGIC" LAYER="3" ORDER="1">
      <contract>
        <purpose>Validate tool input, apply policy normalization with per-country context, execute upstream methods, and map deterministic MCP tool responses.</purpose>
        <inputs>
          <param-1 name="toolName" type="string" />
          <param-2 name="rawArgs" type="unknown" />
          <param-3 name="countryContext" type="CountryContext" />
        </inputs>
        <outputs>
          <param-1 name="McpToolResult" type="object" />
        </outputs>
        <errors>
          <error-VALIDATION_ERROR code="VALIDATION_ERROR" />
          <error-UNSUPPORTED_TOOL code="UNSUPPORTED_TOOL" />
          <error-UPSTREAM_ERROR code="UPSTREAM_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-executeTool PURPOSE="Main proxy entrypoint for all registered tools; accepts CountryContext with per-country chatIds." />
        <type-CountryContext PURPOSE="Per-country context: { chatIds: string[] } resolved from country_settings at runtime." />
      </interface>
      <depends>M-CONFIG, M-TOOLS-CONTRACTS, M-TG-CHAT-RAG-CLIENT, M-LOGGER</depends>
      <notes>
        <note-1>buildUpstreamPayloadWithPolicy uses countryContext.chatIds instead of config.tgChatRag.chatIds for search_messages.</note-1>
      </notes>
    </M-TOOL-PROXY>

    <M-ADMIN-AUTH NAME="AdminAuth" TYPE="CORE_LOGIC" LAYER="3" ORDER="2">
      <contract>
        <purpose>Authenticate admin operators with `ROOT_AUTH_TOKEN` and protect `/admin` routes via signed cookie session checks.</purpose>
        <inputs>
          <param-1 name="request" type="Request" />
          <param-2 name="loginToken" type="string" />
        </inputs>
        <outputs>
          <param-1 name="sessionCookie" type="string" />
          <param-2 name="isAuthenticated" type="boolean" />
        </outputs>
        <errors>
          <error-ADMIN_AUTH_ERROR code="ADMIN_AUTH_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-authenticateAdmin PURPOSE="Validate root token on `/admin/login` and issue session cookie." />
        <export-requireAdminSession PURPOSE="Allow or redirect for `/admin/*` routes." />
        <export-clearAdminSession PURPOSE="Invalidate admin auth cookie on logout." />
      </interface>
      <depends>M-CONFIG, M-LOGGER</depends>
    </M-ADMIN-AUTH>

    <M-PORTAL-AUTH NAME="PortalAuth" TYPE="CORE_LOGIC" LAYER="3" ORDER="3">
      <contract>
        <purpose>Issue and validate tester portal session cookies and enforce portal-route authorization checks.</purpose>
        <inputs>
          <param-1 name="request" type="Request" />
          <param-2 name="identityPayload" type="object" />
        </inputs>
        <outputs>
          <param-1 name="portalSessionDecision" type="object" />
        </outputs>
        <errors>
          <error-PORTAL_AUTH_ERROR code="PORTAL_AUTH_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-authenticatePortalUser PURPOSE="Create portal session from successful social OAuth callback identity result." />
        <export-requirePortalSession PURPOSE="Allow or redirect for protected `/portal/*` routes." />
        <export-clearPortalSession PURPOSE="Invalidate tester portal session cookie." />
      </interface>
      <depends>M-CONFIG, M-LOGGER</depends>
    </M-PORTAL-AUTH>

    <M-PORTAL-PROVISIONING NAME="PortalProvisioning" TYPE="CORE_LOGIC" LAYER="3" ORDER="4">
      <contract>
        <purpose>Coordinate first-login provisioning to guarantee tester MCP access entitlements are assigned automatically.</purpose>
        <inputs>
          <param-1 name="portalUser" type="object" />
        </inputs>
        <outputs>
          <param-1 name="provisioningResult" type="object" />
        </outputs>
        <errors>
          <error-PROVISIONING_ERROR code="PROVISIONING_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-provisionTesterAccess PURPOSE="Ensure identity-provider role/policy grants for MCP testing access are present." />
      </interface>
      <depends>M-PORTAL-IDENTITY, M-LOGGER</depends>
    </M-PORTAL-PROVISIONING>

    <M-DB NAME="DrizzleDb" TYPE="DATA_LAYER" LAYER="1" ORDER="2">
      <contract>
        <purpose>Create Drizzle PostgreSQL client from DATABASE_URL with fail-fast connectivity probe.</purpose>
        <inputs>
          <param-1 name="config" type="AppConfig" />
          <param-2 name="logger" type="Logger" />
        </inputs>
        <outputs>
          <param-1 name="DbClient" type="{ db: NodePgDatabase; pool: Pool }" />
        </outputs>
        <errors>
          <error-DB_CONNECTION_ERROR code="DB_CONNECTION_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-createDb PURPOSE="Validate DATABASE_URL, initialize pg Pool + Drizzle, verify connectivity, and return DbClient." />
        <type-DbClient PURPOSE="Typed Drizzle database and pg Pool handles for repository modules." />
      </interface>
      <depends>M-CONFIG, M-LOGGER</depends>
      <notes>
        <note-1>Reactivated from deprecated status to support M-USAGE-TRACKER persistence. Existing implementation at src/db/index.ts is fully functional.</note-1>
      </notes>
    </M-DB>

    <M-USAGE-TRACKER NAME="UsageTracker" TYPE="DATA_LAYER" LAYER="2" ORDER="4">
      <contract>
        <purpose>Track per-user per-country MCP tool call counts in PostgreSQL and expose query interface for portal display.</purpose>
        <inputs>
          <param-1 name="userId" type="string" />
          <param-2 name="toolName" type="string" />
          <param-3 name="countryCode" type="string" />
        </inputs>
        <outputs>
          <param-1 name="UserUsageStats" type="{ tools: { toolName: string; countryCode: string; callCount: number; lastCalledAt: Date | null }[]; total: number }" />
        </outputs>
        <errors>
          <error-USAGE_TRACKER_ERROR code="USAGE_TRACKER_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-createUsageTracker PURPOSE="Build usage tracker backed by Drizzle PostgreSQL with auto-schema bootstrap." />
        <export-recordToolCall PURPOSE="Increment per-user per-tool per-country counter via UPSERT; fire-and-forget with warning on failure." />
        <export-getUserStats PURPOSE="Query all per-tool per-country counters and total for a given user ID, with optional countryCode filter." />
        <type-UsageTracker PURPOSE="Usage tracking interface for tool call recording and stats queries." />
        <type-UserUsageStats PURPOSE="Per-user usage statistics payload for portal rendering with country breakdown." />
        <schema-usageCountersTable PURPOSE="Drizzle pg-core schema: usage_counters(user_id, tool_name, country_code, call_count, last_called_at) with composite PK." />
      </interface>
      <depends>M-DB, M-DB-SCHEMA, M-LOGGER</depends>
      <notes>
        <note-1>Schema uses composite primary key (user_id, tool_name, country_code) with UPSERT for atomic counter increment.</note-1>
        <note-2>recordToolCall is designed to never throw to callers; failures are caught internally and logged as warnings.</note-2>
      </notes>
    </M-USAGE-TRACKER>

    <M-SITE-SOURCES NAME="SiteSources" TYPE="DATA_LAYER" LAYER="1" ORDER="3">
      <contract>
        <purpose>Serve curated source registry from `site_sources` table filtered by country_code, with bootstrap seed fallback for initialization/migrations.</purpose>
        <inputs>
          <param-1 name="filters" type="{ sourceIds?: string[]; status?: string; countryCode?: string }" />
        </inputs>
        <outputs>
          <param-1 name="SiteSourcesResponse" type="{ description_and_tiers: DescriptionAndTiers; sources: SiteSource[] }" />
        </outputs>
        <errors>
          <error-SITE_SOURCES_ERROR code="SITE_SOURCES_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-SITE_SOURCES_RESPONSE PURPOSE="Bootstrap-only seed constant for initialization/tests (not runtime source of truth)." />
        <export-getSiteSources PURPOSE="Read registry rows from `site_sources` table filtered by country_code for local get_site_sources tool." />
        <export-upsertSiteSourcesSeed PURPOSE="Seed/update initial `site_sources` rows from bootstrap constant." />
        <export-GetSiteSourcesInputSchema PURPOSE="Schema with required country_code for FastMCP tool registration." />
        <type-SiteSourcesResponse PURPOSE="Typed output: description_and_tiers + sources array." />
        <type-SiteSource PURPOSE="Individual source entry: source_id, name, domain, tier, language, focus, status, crawl_interval_minutes, max_pages." />
        <type-TierDescription PURPOSE="Tier metadata: tier number, name, focus description." />
        <type-DescriptionAndTiers PURPOSE="Description text + tiers array." />
      </interface>
      <depends>M-DB, M-DB-SCHEMA, M-LOGGER</depends>
      <notes>
        <note-1>Runtime source of truth is PostgreSQL `site_sources`; bootstrap constant is seed-only.</note-1>
        <note-2>Tool takes no input parameters; returns registry metadata only (no page/chunk content).</note-2>
        <note-3>This is a local tool and must not be added to proxied allowlist.</note-3>
      </notes>
    </M-SITE-SOURCES>

    <M-COUNTRY-SETTINGS NAME="CountrySettings" TYPE="DATA_LAYER" LAYER="1" ORDER="4">
      <contract>
        <purpose>Manage country_settings table CRUD and provide in-memory cache of active countries for tool schema construction and runtime routing.</purpose>
        <inputs>
          <param-1 name="countryCode" type="string" />
          <param-2 name="status" type="CountryStatus" />
          <param-3 name="settings" type="Record&lt;string, unknown&gt;" />
        </inputs>
        <outputs>
          <param-1 name="CountrySettings" type="{ countryCode: string; status: CountryStatus; settings: Record&lt;string, unknown&gt; } | null" />
          <param-2 name="countries" type="CountrySettings[]" />
          <param-3 name="countryCache" type="Map&lt;string, CountrySettings&gt;" />
        </outputs>
        <errors>
          <error-COUNTRY_SETTINGS_ERROR code="COUNTRY_SETTINGS_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-getCountrySettings PURPOSE="Get single country settings by country_code; returns null if not found." />
        <export-getCountriesByStatus PURPOSE="List countries filtered by status (draft/active/coming_soon/maintenance)." />
        <export-upsertCountrySettings PURPOSE="Create or update country settings row with onConflictDoNothing for seed." />
        <export-buildCountryCache PURPOSE="Load active countries into in-memory Map for startup initialization and tool schema enum construction." />
        <type-CountrySettings PURPOSE="Country config record: countryCode, status, settings (JSONB with tg_chat_ids etc)." />
        <type-CountryStatus PURPOSE="String union: 'draft' | 'active' | 'coming_soon' | 'maintenance'." />
        <type-CountryCache PURPOSE="In-memory Map&lt;string, CountrySettings&gt; for fast runtime lookups." />
      </interface>
      <depends>M-DB, M-DB-SCHEMA, M-LOGGER</depends>
      <notes>
        <note-1>Cache is built at server startup from getCountriesByStatus("active"); adding a new country requires restart.</note-1>
        <note-2>settings JSONB holds tg_chat_ids array and future per-country config (display prefs, crawl overrides, etc.).</note-2>
        <note-3>Status lifecycle: draft → active (tool calls allowed), coming_soon (portal teaser), maintenance (temporarily down).</note-3>
      </notes>
    </M-COUNTRY-SETTINGS>

    <M-SPIDER-CLOUD-CLIENT NAME="SpiderCloudClient" TYPE="INTEGRATION" LAYER="2" ORDER="5">
      <contract>
        <purpose>Call Spider crawl API through unified proxy for scheduled and targeted recrawl ingestion jobs.</purpose>
        <inputs>
          <param-1 name="crawlRequest" type="object" />
        </inputs>
        <outputs>
          <param-1 name="crawlResponse" type="object" />
        </outputs>
        <errors>
          <error-SPIDER_PROXY_ERROR code="SPIDER_PROXY_ERROR" />
          <error-SPIDER_TIMEOUT code="SPIDER_TIMEOUT" />
        </errors>
      </contract>
      <interface>
        <export-runCrawl PURPOSE="POST Spider crawl request via `${PROXY_BASE_URL}/api.spider.cloud/*` with proxy and provider auth headers." />
      </interface>
      <depends>M-CONFIG, M-LOGGER</depends>
    </M-SPIDER-CLOUD-CLIENT>

    <M-VOYAGE-PROXY-CLIENT NAME="VoyageProxyClient" TYPE="INTEGRATION" LAYER="2" ORDER="6">
      <contract>
        <purpose>Call Voyage embeddings endpoint through unified proxy for document and query embedding paths.</purpose>
        <inputs>
          <param-1 name="embeddingRequest" type="object" />
        </inputs>
        <outputs>
          <param-1 name="embeddingResponse" type="object" />
        </outputs>
        <errors>
          <error-VOYAGE_PROXY_ERROR code="VOYAGE_PROXY_ERROR" />
          <error-VOYAGE_TIMEOUT code="VOYAGE_TIMEOUT" />
        </errors>
      </contract>
      <interface>
        <export-embedDocuments PURPOSE="Batch document embeddings using `voyage-4` and `input_type=document`." />
        <export-embedQuery PURPOSE="Single query embedding using `voyage-4` and `input_type=query`." />
      </interface>
      <depends>M-CONFIG, M-LOGGER</depends>
    </M-VOYAGE-PROXY-CLIENT>

    <M-SITES-INDEX-REPOSITORY NAME="SitesIndexRepository" TYPE="DATA_LAYER" LAYER="2" ORDER="7">
      <contract>
        <purpose>Persist and query curated-site pages, chunks, embeddings, crawl jobs, and active index version metadata.</purpose>
        <inputs>
          <param-1 name="dbInput" type="object" />
        </inputs>
        <outputs>
          <param-1 name="dbResult" type="object" />
        </outputs>
        <errors>
          <error-SITES_INDEX_REPOSITORY_ERROR code="SITES_INDEX_REPOSITORY_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-upsertPage PURPOSE="Insert/update normalized page by canonical URL + content hash." />
        <export-upsertChunks PURPOSE="Insert/update deterministic chunks with chunking/index versions." />
        <export-upsertEmbeddings PURPOSE="Store chunk embeddings and embedding metadata for active index version." />
        <export-searchHybrid PURPOSE="Execute vector + FTS retrieval with active index_version filter and optional country_code filter via site_sources join." />
        <export-getChunkWithNeighbors PURPOSE="Fetch bounded chunk excerpt and optional neighbors by chunk_id." />
      </interface>
      <depends>M-DB, M-LOGGER</depends>
    </M-SITES-INDEX-REPOSITORY>

    <M-SITES-PARSER-CLEANUP-COMMON NAME="SitesParserCleanupCommon" TYPE="UTILITY" LAYER="3" ORDER="7">
      <contract>
        <purpose>Provide reusable deterministic cleanup primitives for image placeholders, social rows, legal/footer boilerplate, and whitespace normalization.</purpose>
        <inputs>
          <param-1 name="text" type="string" />
        </inputs>
        <outputs>
          <param-1 name="cleanedText" type="string" />
        </outputs>
        <errors>
          <error-SITES_PARSER_CLEANUP_COMMON_ERROR code="SITES_PARSER_CLEANUP_COMMON_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-stripSvgPlaceholders PURPOSE="Remove markdown image placeholders using data:image/svg+xml and similar non-content placeholders." />
        <export-stripSocialShareRows PURPOSE="Remove standalone social-share rows and empty share-button link lists." />
        <export-stripLegalFooterRows PURPOSE="Remove legal/footer rows (copyright, all-rights-reserved, cookie banners) when they are standalone boilerplate." />
        <export-normalizeCleanupWhitespace PURPOSE="Collapse excessive blank lines and trim normalized output." />
      </interface>
      <depends>M-LOGGER</depends>
    </M-SITES-PARSER-CLEANUP-COMMON>

    <M-SITES-PARSER-CLEANUP-PROFILES NAME="SitesParserCleanupProfiles" TYPE="CORE_LOGIC" LAYER="3" ORDER="8">
      <contract>
        <purpose>Compose source-family cleanup profiles (editorial/community/transit-official) from common deterministic primitives.</purpose>
        <inputs>
          <param-1 name="text" type="string" />
          <param-2 name="sourceId" type="string" />
        </inputs>
        <outputs>
          <param-1 name="profileCleanedText" type="string" />
        </outputs>
        <errors>
          <error-SITES_PARSER_CLEANUP_PROFILE_ERROR code="SITES_PARSER_CLEANUP_PROFILE_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-applyEditorialProfile PURPOSE="Apply conservative cleanup profile for guide/editorial sites (insidekyoto, trulytokyo, kansai_odyssey, invisible_tourist, japan_unravelled, japan_guide)." />
        <export-applyTransitOfficialProfile PURPOSE="Apply conservative cleanup profile for transit/official sites (navitime, jorudan, jreast, smart_ex)." />
        <export-applyCommunityProfile PURPOSE="Apply community cleanup profile preserving user narratives while removing shell/footer rows (reddit_japantravel)." />
      </interface>
      <depends>M-SITES-PARSER-CLEANUP-COMMON, M-LOGGER</depends>
    </M-SITES-PARSER-CLEANUP-PROFILES>

    <M-SITES-PARSER-CLEANUP-ADAPTERS-REMAINING NAME="SitesParserRemainingAdapters" TYPE="CORE_LOGIC" LAYER="3" ORDER="9">
      <contract>
        <purpose>Define Layer B deterministic source adapters for all curated sources except wrenjapan using fixture-backed rules.</purpose>
        <inputs>
          <param-1 name="text" type="string" />
          <param-2 name="sourceId" type="'insidekyoto' | 'trulytokyo' | 'kansai_odyssey' | 'invisible_tourist' | 'japan_unravelled' | 'japan_guide' | 'reddit_japantravel' | 'navitime' | 'jorudan' | 'jreast' | 'smart_ex'" />
        </inputs>
        <outputs>
          <param-1 name="adapter" type="SourceAdapter" />
        </outputs>
        <errors>
          <error-SITES_PARSER_ADAPTER_ERROR code="SITES_PARSER_ADAPTER_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-insidekyotoAdapter PURPOSE="Remove gallery/image-placeholder and non-article scaffolding while preserving itinerary content." />
        <export-trulytokyoAdapter PURPOSE="Remove booking-widget/image-placeholder boilerplate while preserving district/hotel guidance." />
        <export-kansaiOdysseyAdapter PURPOSE="Remove category/menu boilerplate while preserving destination narratives." />
        <export-invisibleTouristAdapter PURPOSE="Remove newsletter/share CTA blocks while preserving itinerary and logistics content." />
        <export-japanUnravelledAdapter PURPOSE="Remove repeated Substack share sections while preserving article body content." />
        <export-japanGuideAdapter PURPOSE="Apply conservative reference-site cleanup for low-noise pages." />
        <export-redditJapantravelAdapter PURPOSE="Remove reddit shell/share/legal rows while preserving thread narrative content." />
        <export-navitimeAdapter PURPOSE="Remove route-tool shell noise while preserving route and fare guidance." />
        <export-jorudanAdapter PURPOSE="Remove transit service shell/legal blocks while preserving schedule and pass information." />
        <export-jreastAdapter PURPOSE="Remove corporate shell/navigation boilerplate while preserving official railway information." />
        <export-smartExAdapter PURPOSE="Remove app/tutorial shell rows while preserving reservation workflow content." />
      </interface>
      <depends>M-SITES-PARSER-CLEANUP-PROFILES, M-LOGGER</depends>
    </M-SITES-PARSER-CLEANUP-ADAPTERS-REMAINING>

    <M-SITES-PARSER-CLEANUP-REGISTRY NAME="SitesParserCleanupRegistry" TYPE="UTILITY" LAYER="3" ORDER="10">
      <contract>
        <purpose>Provide a deterministic source_id-to-adapter registry covering wrenjapan and all remaining curated sources.</purpose>
        <inputs>
          <param-1 name="sourceId" type="string" />
        </inputs>
        <outputs>
          <param-1 name="adapterOrUndefined" type="SourceAdapter | undefined" />
        </outputs>
        <errors>
          <error-SITES_PARSER_CLEANUP_REGISTRY_ERROR code="SITES_PARSER_CLEANUP_REGISTRY_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-getCleanupAdapter PURPOSE="Resolve source adapter by source_id from stable registry map." />
        <const-SUPPORTED_CLEANUP_SOURCE_IDS PURPOSE="Stable list of all source_ids with Layer B adapter coverage." />
      </interface>
      <depends>M-SITES-PARSER-CLEANUP-ADAPTERS-REMAINING, M-LOGGER</depends>
      <notes>
        <note-1>Registry includes existing `wrenjapan` adapter and all remaining source adapters for complete Layer B coverage.</note-1>
      </notes>
    </M-SITES-PARSER-CLEANUP-REGISTRY>

    <M-SITES-PARSER NAME="SitesParser" TYPE="CORE_LOGIC" LAYER="3" ORDER="5">
      <contract>
        <purpose>Normalize Spider payload into canonical page records with layered content cleanup (global → source adapter → quality gate) and discriminated union output (accepted vs skipped).</purpose>
        <inputs>
          <param-1 name="crawlPayload" type="SpiderCrawlItem" />
          <param-2 name="sourceId" type="string" />
          <param-3 name="logger" type="Logger" />
        </inputs>
        <outputs>
          <param-1 name="ParserResult" type="{ status: 'accepted', page: ParsedPage } | { status: 'skipped', reason: SkipReason, source_id: string, url: string, metrics?: CleanupMetrics }" />
        </outputs>
        <errors>
          <error-SITES_PARSER_ERROR code="SITES_PARSER_ERROR" description="Thrown only for truly invalid inputs (malformed URL, malformed crawl payload). Empty/low-quality content returns structured skip instead." />
        </errors>
      </contract>
      <interface>
        <export-parseCrawlItem PURPOSE="Parse crawl item through layered cleanup pipeline and return discriminated union (accepted page or structured skip with reason code)." />
        <export-runCleanupPipeline PURPOSE="Orchestrate Layer A (global cleanup) → Layer B (source adapter) → Layer C (quality gate) and return CleanupResult." />
        <export-getCleanupAdapter PURPOSE="Resolve source adapter for Layer B from registry that covers all active curated source_ids." />
        <export-globalCleanup PURPOSE="Apply universal noise removal: data-URI images, empty formatting fragments, social-share rows, whitespace normalization." />
        <export-qualityGate PURPOSE="Compute post-cleanup metrics and decide accept vs skip with reason code (EMPTY_AFTER_CLEANUP, TOO_SHORT_AFTER_CLEANUP)." />
        <export-wrenjapanAdapter PURPOSE="Source-specific cleanup for wrenjapan: strip nav/menu blocks, newsletter promos, image placeholders, footer navigation." />
        <export-insidekyotoAdapter PURPOSE="Source-specific cleanup for insidekyoto fixture patterns and gallery placeholders." />
        <export-trulytokyoAdapter PURPOSE="Source-specific cleanup for trulytokyo booking/image placeholder patterns." />
        <export-kansaiOdysseyAdapter PURPOSE="Source-specific cleanup for kansai_odyssey category/menu boilerplate." />
        <export-invisibleTouristAdapter PURPOSE="Source-specific cleanup for invisible_tourist share/newsletter blocks." />
        <export-japanUnravelledAdapter PURPOSE="Source-specific cleanup for japan_unravelled share boilerplate." />
        <export-japanGuideAdapter PURPOSE="Source-specific conservative cleanup for japan_guide reference pages." />
        <export-redditJapantravelAdapter PURPOSE="Source-specific cleanup for reddit_japantravel shell/legal/share rows." />
        <export-navitimeAdapter PURPOSE="Source-specific cleanup for navitime route tool shell noise." />
        <export-jorudanAdapter PURPOSE="Source-specific cleanup for jorudan transit-service shell rows." />
        <export-jreastAdapter PURPOSE="Source-specific cleanup for jreast official-site shell rows." />
        <export-smartExAdapter PURPOSE="Source-specific cleanup for smart_ex booking/tutorial shell rows." />
        <type-ParsedPage PURPOSE="Canonical page record: source_id, url, canonical_url, title, clean_text, text_hash, http_status, fetched_at." />
        <type-ParserResult PURPOSE="Discriminated union of accepted page or structured skip." />
        <type-SkipReason PURPOSE="String enum: EMPTY_CONTENT, EMPTY_AFTER_CLEANUP, TOO_SHORT_AFTER_CLEANUP." />
        <type-CleanupMetrics PURPOSE="Post-cleanup quality metrics: clean_char_count." />
        <type-SourceAdapter PURPOSE="Interface: { sourceId: string; clean(text: string): string }." />
        <const-MIN_CLEAN_CHARS PURPOSE="Quality gate threshold (v1 default: 80 chars)." />
      </interface>
      <depends>M-LOGGER, M-SITES-PARSER-CLEANUP-REGISTRY</depends>
      <notes>
        <note-1>Cleanup code is extracted into `src/sites/parser/cleanup/` subdirectory: index.ts (orchestration), global.ts, quality-gate.ts, types.ts, common.ts, profiles.ts, registry.ts, and adapters/* per source.</note-1>
        <note-2>Source adapter selection is by source_id only and covers all active curated sources. Adapter interface remains intentionally simple (string → string).</note-2>
        <note-3>Title extraction runs on post-cleanup text (Layer A+B) with fallback to raw content, then URL-derived title.</note-3>
        <note-4>Empty source content becomes structured skip (EMPTY_CONTENT) instead of thrown SitesParserError.</note-4>
        <note-5>Quality gate thresholds are intentionally soft in v1 to avoid over-skipping; tuned via fixture-based analysis.</note-5>
      </notes>
    </M-SITES-PARSER>

    <M-SITES-CHUNKER NAME="SitesChunker" TYPE="CORE_LOGIC" LAYER="3" ORDER="6">
      <contract>
        <purpose>Split parsed page text with deterministic structural/token strategy and stable chunk metadata.</purpose>
        <inputs>
          <param-1 name="pageText" type="string" />
        </inputs>
        <outputs>
          <param-1 name="chunks" type="object[]" />
        </outputs>
        <errors>
          <error-SITES_CHUNKER_ERROR code="SITES_CHUNKER_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-chunkPage PURPOSE="Apply v1 split config (`target=450`, `max=650`, `overlap=80`) using npm `tiktoken` token counting and output ordered chunks." />
      </interface>
      <depends>M-LOGGER</depends>
      <notes>
        <note-1>Token counting strategy is fixed to `tiktoken`; chars/4 approximation is out of scope for v1.</note-1>
      </notes>
    </M-SITES-CHUNKER>

    <M-SITES-INGESTION NAME="SitesIngestionOrchestrator" TYPE="CORE_LOGIC" LAYER="4" ORDER="3">
      <contract>
        <purpose>Run scheduled and targeted crawl-only ingestion jobs, handle parser quality-skip results as non-fatal structured outcomes, and persist parsed/chunked/embedded index artifacts.</purpose>
        <inputs>
          <param-1 name="ingestionJob" type="object" />
        </inputs>
        <outputs>
          <param-1 name="ingestionResult" type="IngestionResult" description="Includes pages_skipped counter; errors[] contains only true failures, not quality skips." />
        </outputs>
        <errors>
          <error-SITES_INGESTION_ERROR code="SITES_INGESTION_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-runScheduledIngestion PURPOSE="Select due sources by interval and execute crawl ingestion; accumulates pages_skipped for quality-gated pages." />
        <export-runTargetedRecrawl PURPOSE="Execute internal single-URL recrawl via crawl endpoint with strict cap; handles parser skip results." />
        <type-IngestionResult PURPOSE="Aggregated counters: sources_processed, pages_fetched, pages_skipped, chunks_created, embeddings_created, errors[]." />
        <type-IngestionError PURPOSE="Typed error record for true failures only; quality skips are tracked via pages_skipped counter." />
      </interface>
      <depends>M-SITE-SOURCES, M-SPIDER-CLOUD-CLIENT, M-SITES-PARSER, M-SITES-CHUNKER, M-VOYAGE-PROXY-CLIENT, M-SITES-INDEX-REPOSITORY, M-LOGGER</depends>
      <notes>
        <note-1>Parser skip decisions (EMPTY_CONTENT, EMPTY_AFTER_CLEANUP, TOO_SHORT_AFTER_CLEANUP) do not populate errors[]; they increment pages_skipped and are logged as info/warn with reason code.</note-1>
        <note-2>Both runScheduledIngestion and runTargetedRecrawl handle the parser discriminated union; only accepted pages proceed to chunking/embedding.</note-2>
      </notes>
    </M-SITES-INGESTION>

    <M-SITES-SEARCH NAME="SitesSearchService" TYPE="CORE_LOGIC" LAYER="4" ORDER="4">
      <contract>
        <purpose>Serve local MCP curated-site search tools with hybrid retrieval and bounded evidence chunk access.</purpose>
        <inputs>
          <param-1 name="searchArgs" type="object" />
          <param-2 name="chunkArgs" type="object" />
        </inputs>
        <outputs>
          <param-1 name="searchResults" type="object[]" />
          <param-2 name="chunkResult" type="object" />
        </outputs>
        <errors>
          <error-SITES_SEARCH_ERROR code="SITES_SEARCH_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-searchSites PURPOSE="Hybrid search over active index_version returning attributed snippets." />
        <export-getPageChunk PURPOSE="Return bounded excerpt by chunk_id with optional neighbors." />
      </interface>
      <depends>M-VOYAGE-PROXY-CLIENT, M-SITES-INDEX-REPOSITORY, M-LOGGER</depends>
    </M-SITES-SEARCH>

    <M-ADMIN-UI NAME="AdminUiRoutes" TYPE="UI_COMPONENT" LAYER="4" ORDER="1">
      <contract>
        <purpose>Render admin login, ops diagnostics, and sites management pages with root-token auth workflow and HTMX progressive enhancement.</purpose>
        <inputs>
          <param-1 name="request" type="Request" />
        </inputs>
        <outputs>
          <param-1 name="response" type="Response" />
        </outputs>
        <errors>
          <error-ADMIN_UI_ERROR code="ADMIN_UI_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-handleAdminRequest PURPOSE="Handle `/admin/login`, `/admin/ops`, and `/admin/sites/*` endpoints." />
        <export-renderAdminLayout PURPOSE="Render HTML layout for admin pages with sidebar (Ops Diagnostics + Sites Management tabs)." />
        <export-renderOpsStatus PURPOSE="Render runtime diagnostic fragment." />
      </interface>
      <depends>M-ADMIN-AUTH, M-ADMIN-SITES, M-CONFIG, M-LOGGER, M-DB</depends>
    </M-ADMIN-UI>

    <M-ADMIN-SITES NAME="AdminSitesPage" TYPE="UI_COMPONENT" LAYER="4" ORDER="5">
      <contract>
        <purpose>Query curated site source data with per-source index statistics from PostgreSQL and render Sites Management admin page with full CRUD, pause/resume, and cascade delete operations.</purpose>
        <inputs>
          <param-1 name="db" type="NodePgDatabase" />
          <param-2 name="sourceInput" type="object" />
          <param-3 name="sourceId" type="string" />
        </inputs>
        <outputs>
          <param-1 name="sitesPageData" type="SitesPageData" />
          <param-2 name="contentHtml" type="string" />
        </outputs>
        <errors>
          <error-ADMIN_SITES_ERROR code="ADMIN_SITES_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-fetchSitesPageData PURPOSE="Query all sources with aggregated per-source stats (page_count, chunk_count, embedding_count, last_crawl_at) and recent crawl jobs." />
        <export-renderSitesContent PURPOSE="Render sources table with status badges, stats columns, and action buttons (edit/pause/resume/delete)." />
        <export-renderSourceForm PURPOSE="Render create/edit form with Zod-validated fields (source_id, name, domain, tier, language, focus, crawl_interval_minutes, max_pages)." />
        <export-handleCreateSource PURPOSE="Validate and INSERT new source into site_sources table." />
        <export-handleUpdateSource PURPOSE="Validate and UPDATE existing source metadata fields." />
        <export-handleDeleteSource PURPOSE="CASCADE delete source with all related embeddings, chunks, pages, and crawl jobs." />
        <export-handleToggleSourceStatus PURPOSE="Toggle source status between active and paused." />
        <export-CreateSourceInputSchema PURPOSE="Zod schema for create source validation (source_id required, 3-50 chars, alphanumeric+underscore)." />
        <export-UpdateSourceInputSchema PURPOSE="Zod schema for edit source validation (source_id not editable)." />
        <type-SiteSourceRow PURPOSE="Source metadata + aggregated stats (page_count, chunk_count, embedding_count, last_crawl_at)." />
        <type-CrawlJobRow PURPOSE="Recent crawl job summary (crawl_job_id, source_id, status, started_at, finished_at, pages_fetched, error)." />
        <type-SitesPageData PURPOSE="Full page data model: sources list + recent crawl jobs." />
      </interface>
      <depends>M-DB, M-LOGGER</depends>
      <notes>
        <note-1>Delete cascade order: site_chunk_embeddings → site_chunks → site_pages → site_crawl_jobs → site_sources.</note-1>
        <note-2>source_id is immutable after creation (validated on create, excluded from edit form).</note-2>
        <note-3>Form validation uses Zod: domain format, tier 0-2, crawl_interval min 60, max_pages 1-1000.</note-3>
        <note-4>Rendering follows existing admin pattern: raw HTML strings, no JSX, inline CSS, HTMX progressive enhancement.</note-4>
      </notes>
    </M-ADMIN-SITES>

    <M-PORTAL-UI NAME="PortalUiRoutes" TYPE="UI_COMPONENT" LAYER="4" ORDER="2">
      <contract>
        <purpose>Render public landing and user-portal pages with platform branding (PLATFORM_NAME), per-page route handlers, expose social OAuth entrypoints/callback flow, render MCP setup instructions with single-endpoint multi-country guidance, display per-user usage statistics, and show destination list from country_settings on portal home.</purpose>
        <inputs>
          <param-1 name="request" type="Request" />
        </inputs>
        <outputs>
          <param-1 name="response" type="Response" />
        </outputs>
        <errors>
          <error-PORTAL_UI_ERROR code="PORTAL_UI_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-handleLandingRequest PURPOSE="Serve `/` landing with primary redirect action to `/portal`." />
        <export-handlePortalRootRoute PURPOSE="Handle `GET /portal` and route to login/home by session state." />
        <export-handlePortalRegisterRoute PURPOSE="Handle `GET /portal/register` social-provider onboarding page without password fields." />
        <export-handlePortalLoginRoute PURPOSE="Handle `GET /portal/login` social-provider login page without password fields." />
        <export-handlePortalOauthStartRoute PURPOSE="Handle `GET /portal/auth/start` and redirect to Logto social OAuth authorization endpoint." />
        <export-handlePortalOauthCallbackRoute PURPOSE="Handle `GET /portal/auth/callback` code exchange, provisioning, and portal session creation." />
        <export-handlePortalHomeRoute PURPOSE="Handle `GET /portal/home` authenticated onboarding page with per-user usage statistics and available destinations list." />
        <export-handlePortalAgentSetupRoute PURPOSE="Handle `GET /portal/integrations/agent-setup` detailed MCP setup guide page." />
        <export-handlePortalLogoutRoute PURPOSE="Handle `POST /portal/logout` and clear portal session." />
        <export-renderPortalLayout PURPOSE="Render shared portal shell and include compiled Tailwind CSS asset." />
        <export-renderPortalConnectionGuide PURPOSE="Render single-endpoint MCP setup instructions with country_code parameter guidance for external AI agents." />
        <export-renderDestinationList PURPOSE="Render available destinations from country_settings with status-appropriate display (active, coming_soon teaser)." />
      </interface>
      <depends>M-CONFIG, M-LOGGER, M-PORTAL-AUTH, M-PORTAL-PROVISIONING, M-PORTAL-IDENTITY, M-USAGE-TRACKER, M-COUNTRY-SETTINGS</depends>
      <notes>
        <note-1>Invite-link flow is intentionally excluded in this phase.</note-1>
        <note-2>Portal UI stack is Bun JSX SSR + Tailwind + HTMX, with Alpine.js only for optional local micro-interactions.</note-2>
        <note-3>Per-page route architecture is mandatory in this phase (no client-side SPA router).</note-3>
        <note-4>Register/login pages are custom but social-only; they route users to OAuth start endpoint rather than collecting password credentials.</note-4>
      </notes>
    </M-PORTAL-UI>

    <M-FASTMCP-RUNTIME NAME="FastMcpRuntime" TYPE="ENTRY_POINT" LAYER="5" ORDER="1">
      <contract>
        <purpose>Create FastMCP server with OAuth Proxy for `/mcp`, register fixed tool set (4 proxied + 3 local) with per-user per-country usage tracking and country_code routing, and mount admin + portal HTTP routes on underlying Hono app.</purpose>
        <inputs>
          <param-1 name="config" type="AppConfig" />
          <param-2 name="logger" type="Logger" />
          <param-3 name="oauthProxy" type="OAuthProxy" />
          <param-4 name="proxyService" type="ToolProxyService" />
          <param-5 name="adminHandler" type="(request: Request) =&gt; Promise&lt;Response&gt;" />
          <param-6 name="portalLandingHandler" type="(request: Request) =&gt; Promise&lt;Response&gt;" />
          <param-7 name="portalHandler" type="(request: Request) =&gt; Promise&lt;Response&gt;" />
          <param-8 name="usageTracker" type="UsageTracker" />
          <param-9 name="sitesSearchService" type="SitesSearchService" />
          <param-10 name="siteSourcesRepository" type="SiteSourcesRepository" />
          <param-11 name="countryCache" type="CountryCache" />
          <param-12 name="toolSchemas" type="ToolSchemas" />
        </inputs>
        <outputs>
          <param-1 name="fastMcpServer" type="FastMCP" />
        </outputs>
        <errors>
          <error-FASTMCP_RUNTIME_ERROR code="FASTMCP_RUNTIME_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-createFastMcpRuntime PURPOSE="Build FastMCP with oauth.enabled=true and mount non-MCP HTTP routes via getApp()." />
        <export-registerProxyTools PURPOSE="Register the four proxied tools with country_code-aware schemas, canAccess guards, per-country chat_id resolution from countryCache, and per-user per-country usage tracking." />
        <export-registerLocalTools PURPOSE="Register local tools (`get_site_sources`, `search_sites`, `get_page_chunk`) with country_code-aware schemas, canAccess guards, and per-user per-country usage tracking." />
        <export-extractUserIdFromSession PURPOSE="Decode JWT sub claim from RuntimeOauthSession idToken or accessToken for usage tracking." />
        <export-mountAdminRoutes PURPOSE="Mount `/admin` and `/admin/*` handlers via getApp()." />
        <export-mountPortalRoutes PURPOSE="Mount `/`, `/portal`, and `/portal/*` handlers via getApp()." />
      </interface>
      <depends>M-CONFIG, M-LOGGER, M-AUTH-PROXY, M-TOOLS-CONTRACTS, M-TOOL-PROXY, M-ADMIN-UI, M-PORTAL-UI, M-USAGE-TRACKER, M-SITE-SOURCES, M-SITES-SEARCH, M-COUNTRY-SETTINGS</depends>
      <notes>
        <note-1>Server name changes from "japan-travel-rag-mcp" to "travelmind-mcp".</note-1>
        <note-2>Tool descriptions become generic: "Search curated travel site pages" instead of "Japan travel site pages".</note-2>
      </notes>
    </M-FASTMCP-RUNTIME>

    <M-SERVER NAME="ServerMain" TYPE="ENTRY_POINT" LAYER="6" ORDER="1">
      <contract>
        <purpose>Bootstrap API runtime dependencies including country settings cache, build dynamic tool schemas, and start FastMCP HTTP stream server with MCP, admin, portal, and local curated-site tools.</purpose>
        <inputs>
          <param-1 name="none" type="void" />
        </inputs>
        <outputs>
          <param-1 name="startedServer" type="FastMCP" />
        </outputs>
        <errors>
          <error-SERVER_START_ERROR code="SERVER_START_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-main PURPOSE="Load config, load country settings cache, build dynamic tool schemas, create API dependencies, mount route handlers, start FastMCP runtime, install graceful shutdown." />
      </interface>
      <depends>M-CONFIG, M-LOGGER, M-AUTH-PROXY, M-TG-CHAT-RAG-CLIENT, M-TOOL-PROXY, M-ADMIN-UI, M-ADMIN-SITES, M-PORTAL-IDENTITY, M-PORTAL-AUTH, M-PORTAL-PROVISIONING, M-PORTAL-UI, M-FASTMCP-RUNTIME, M-DB, M-USAGE-TRACKER, M-SITE-SOURCES, M-SITES-SEARCH, M-SITES-INDEX-REPOSITORY, M-VOYAGE-PROXY-CLIENT, M-COUNTRY-SETTINGS, M-DB-SITES-BOOTSTRAP</depends>
      <notes>
        <note-1>After DB bootstrap, loads country settings cache and builds dynamic tool schemas before passing to runtime.</note-1>
      </notes>
    </M-SERVER>

    <M-WORKER-ENTRYPOINT NAME="WorkerMain" TYPE="ENTRY_POINT" LAYER="6" ORDER="2">
      <contract>
        <purpose>Bootstrap worker dependencies and run recurring curated-site scheduling, crawl ingestion, parsing, chunking, and embedding jobs.</purpose>
        <inputs>
          <param-1 name="none" type="void" />
        </inputs>
        <outputs>
          <param-1 name="workerLoop" type="void" />
        </outputs>
        <errors>
          <error-WORKER_START_ERROR code="WORKER_START_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-main PURPOSE="Load config, initialize ingestion dependencies, run scheduler tick loop, handle graceful shutdown." />
      </interface>
      <depends>M-CONFIG, M-LOGGER, M-DB, M-SITE-SOURCES, M-SPIDER-CLOUD-CLIENT, M-VOYAGE-PROXY-CLIENT, M-SITES-PARSER, M-SITES-CHUNKER, M-SITES-INDEX-REPOSITORY, M-SITES-INGESTION</depends>
    </M-WORKER-ENTRYPOINT>

    <M-ROOT-ENTRYPOINT NAME="RootEntrypoint" TYPE="ENTRY_POINT" LAYER="7" ORDER="1">
      <contract>
        <purpose>Call API entrypoint by default and map fatal startup errors to process exit behavior.</purpose>
        <inputs>
          <param-1 name="none" type="void" />
        </inputs>
        <outputs>
          <param-1 name="processExit" type="void" />
        </outputs>
        <errors>
          <error-BOOTSTRAP_ERROR code="BOOTSTRAP_ERROR" />
        </errors>
      </contract>
      <interface>
        <export-bootstrap PURPOSE="Invoke API main() by default and handle fatal startup exceptions." />
      </interface>
      <depends>M-SERVER</depends>
    </M-ROOT-ENTRYPOINT>
  </Modules>

  <DataFlows>
    <DF-LANDING-TO-PORTAL NAME="public landing redirect flow">
      <step-1>Visitor opens `/` and receives concise landing page.</step-1>
      <step-2>Primary CTA redirects visitor to `/portal` (or directly `/portal/register`).</step-2>
      <step-3>No invite-link precondition is checked in this phase.</step-3>
    </DF-LANDING-TO-PORTAL>

    <DF-PORTAL-REGISTRATION NAME="portal registration flow">
      <step-1>Tester opens `/portal/register` and chooses a social provider.</step-1>
      <step-2>Route `/portal/auth/start` builds provider-specific OAuth URL (direct social sign-in) and redirects to Logto.</step-2>
      <step-3>Logto redirects to `/portal/auth/callback`, then M-PORTAL-IDENTITY exchanges code and resolves identity.</step-3>
      <step-4>M-PORTAL-PROVISIONING ensures tester MCP access entitlement; M-PORTAL-AUTH issues portal session and redirects to `/portal/home`.</step-4>
    </DF-PORTAL-REGISTRATION>

    <DF-PORTAL-LOGIN NAME="portal login flow">
      <step-1>Tester opens `/portal/login` and chooses a social provider.</step-1>
      <step-2>Route `/portal/auth/start` redirects to provider login via Logto OAuth flow.</step-2>
      <step-3>`/portal/auth/callback` resolves identity and M-PORTAL-PROVISIONING re-validates tester access entitlements.</step-3>
      <step-4>M-PORTAL-AUTH issues session and portal home renders MCP setup guide.</step-4>
    </DF-PORTAL-LOGIN>

    <DF-PORTAL-HTMX-FORMS NAME="portal progressive form interaction flow">
      <step-1>User interacts with server-rendered register/login pages containing social provider actions.</step-1>
      <step-2>HTMX requests can update provider selection/error fragments and route to OAuth start endpoint.</step-2>
      <step-3>Route handlers return deterministic HTML fragments/full responses without SPA state management.</step-3>
      <step-4>Alpine.js is used only for optional local UI behaviors that do not replace server authority.</step-4>
    </DF-PORTAL-HTMX-FORMS>

    <DF-PORTAL-E2E-MCP NAME="portal to first mcp usage flow">
      <step-1>Authenticated portal user reads MCP setup details (`/mcp` endpoint + auth requirements).</step-1>
      <step-2>User configures external AI agent with provided instructions.</step-2>
      <step-3>Agent authenticates via OAuth flow on `/mcp` and executes one of active tools (`search_messages` or `search_sites`).</step-3>
    </DF-PORTAL-E2E-MCP>

    <DF-MCP-OAUTH NAME="mcp oauth proxy auth flow">
      <step-1>External MCP client reaches `/mcp` and receives OAuth flow from FastMCP.</step-1>
      <step-2>FastMCP OAuthProxy exchanges auth code with Logto upstream.</step-2>
      <step-3>Tool execution proceeds only for authenticated OAuth session.</step-3>
    </DF-MCP-OAUTH>

    <DF-TOOL-PROXY NAME="tool proxy flow with country routing">
      <step-1>Tool call enters M-FASTMCP-RUNTIME with country_code in args.</step-1>
      <step-2>M-TOOLS-CONTRACTS validates payload including country_code enum membership.</step-2>
      <step-3>M-FASTMCP-RUNTIME resolves CountryContext (tg_chat_ids) from M-COUNTRY-SETTINGS cache.</step-3>
      <step-4>M-TOOL-PROXY applies policy with country-specific chatIds and calls M-TG-CHAT-RAG-CLIENT.</step-4>
      <step-5>Normalized result is returned via MCP transport.</step-5>
    </DF-TOOL-PROXY>

    <DF-USAGE-TRACKING NAME="per-user per-country tool call usage tracking flow">
      <step-1>MCP tool call enters execute handler in M-FASTMCP-RUNTIME with context.session containing RuntimeOauthSession.</step-1>
      <step-2>extractUserIdFromSession decodes idToken (or accessToken) JWT payload and extracts `sub` claim as userId.</step-2>
      <step-3>After successful tool execution, M-USAGE-TRACKER.recordToolCall(userId, toolName, countryCode) performs UPSERT on usage_counters table with composite PK (user_id, tool_name, country_code).</step-3>
      <step-4>If userId extraction or recording fails, tool result is still returned; failure is logged as warning.</step-4>
    </DF-USAGE-TRACKING>

    <DF-PORTAL-USAGE-DISPLAY NAME="portal usage statistics display flow">
      <step-1>Authenticated portal user opens `/portal/home`.</step-1>
      <step-2>M-PORTAL-UI calls M-USAGE-TRACKER.getUserStats(session.sub) to query per-tool counters.</step-2>
      <step-3>Portal home renders usage statistics section with per-tool call counts and total.</step-3>
      <step-4>If stats query fails, portal home renders gracefully without usage section or with error placeholder.</step-4>
    </DF-PORTAL-USAGE-DISPLAY>

    <DF-SITE-SOURCES NAME="get_site_sources local tool flow with country filtering">
      <step-1>Authenticated MCP client calls get_site_sources with country_code parameter.</step-1>
      <step-2>canAccess guard validates OAuth session (same as proxied tools).</step-2>
      <step-3>Execute handler reads curated source registry from `site_sources` table filtered by country_code.</step-3>
      <step-4>Usage tracking fires with userId + "get_site_sources" + countryCode (fire-and-forget).</step-4>
      <step-5>Client receives description_and_tiers + sources registry for specified country.</step-5>
    </DF-SITE-SOURCES>

    <DF-SITES-INGESTION NAME="curated sites crawl ingestion flow">
      <step-1>Worker scheduler selects due sources from `site_sources` by `crawl_interval_minutes`.</step-1>
      <step-2>M-SITES-INGESTION calls M-SPIDER-CLOUD-CLIENT crawl endpoint through unified proxy with source limits.</step-2>
      <step-3>M-SITES-PARSER applies layered cleanup (global → source adapter → quality gate) and returns accepted pages or structured skips with reason codes.</step-3>
      <step-4>M-SITES-INGESTION increments `pages_skipped` for quality-gated pages (non-fatal, logged as info/warn); only accepted pages proceed.</step-4>
      <step-5>M-SITES-CHUNKER applies deterministic chunking and M-VOYAGE-PROXY-CLIENT embeds chunks for accepted pages.</step-5>
      <step-6>M-SITES-INDEX-REPOSITORY upserts pages/chunks/embeddings for active `index_version`.</step-6>
    </DF-SITES-INGESTION>

    <DF-CLEANUP-ADAPTER-MATRIX NAME="all-source cleanup adapter selection flow">
      <step-1>M-SITES-PARSER receives `source_id` from ingestion context.</step-1>
      <step-2>M-SITES-PARSER-CLEANUP-REGISTRY resolves adapter for one of: wrenjapan, insidekyoto, trulytokyo, kansai_odyssey, invisible_tourist, japan_unravelled, japan_guide, reddit_japantravel, navitime, jorudan, jreast, smart_ex.</step-2>
      <step-3>Resolved adapter applies deterministic Layer B rules; if source is unknown, parser falls back to Layer A + Layer C only.</step-3>
      <step-4>Fixture-matrix tests validate noise removal and content preservation per source before rollout recrawl.</step-4>
    </DF-CLEANUP-ADAPTER-MATRIX>

    <DF-SITES-SEARCH NAME="search_sites local tool flow with country filtering">
      <step-1>Authenticated MCP client calls `search_sites` with query, country_code, and optional filters.</step-1>
      <step-2>M-SITES-SEARCH builds query embedding via M-VOYAGE-PROXY-CLIENT (`voyage-4`, query mode).</step-2>
      <step-3>M-SITES-INDEX-REPOSITORY runs hybrid search (vector + FTS simple config) filtered by active `index_version` and `country_code` via site_sources join.</step-3>
      <step-4>Runtime returns ranked snippets with attribution (`source_id`, `tier`, `original_page_url`, `chunk_id`) scoped to the requested country.</step-4>
    </DF-SITES-SEARCH>

    <DF-PAGE-CHUNK NAME="get_page_chunk local tool flow">
      <step-1>Authenticated MCP client calls `get_page_chunk(chunk_id)`.</step-1>
      <step-2>M-SITES-SEARCH resolves chunk and optional neighbors from repository.</step-2>
      <step-3>Response returns bounded evidence excerpt with attribution and no full-page dump.</step-3>
    </DF-PAGE-CHUNK>

    <DF-ADMIN-ROOT-AUTH NAME="admin root token flow">
      <step-1>Operator submits token on `/admin/login`.</step-1>
      <step-2>M-ADMIN-AUTH verifies against `ROOT_AUTH_TOKEN` and issues session cookie.</step-2>
      <step-3>Protected `/admin/ops` routes require valid admin session and never use portal/MCP auth paths.</step-3>
    </DF-ADMIN-ROOT-AUTH>

    <DF-ADMIN-SITES-CRUD NAME="admin sites management CRUD flow">
      <step-1>Authenticated admin operator navigates to `/admin/sites` via sidebar.</step-1>
      <step-2>M-ADMIN-UI dispatches to M-ADMIN-SITES.fetchSitesPageData which queries site_sources with per-source stats via aggregate subqueries.</step-2>
      <step-3>M-ADMIN-SITES.renderSitesContent generates sources table with stats, status badges, and action buttons.</step-3>
      <step-4>Create: operator opens `/admin/sites/new`, fills form, POST validated by CreateSourceInputSchema, INSERT into site_sources, redirect to list.</step-4>
      <step-5>Edit: operator opens `/admin/sites/:id/edit`, modifies fields, POST validated by UpdateSourceInputSchema, UPDATE site_sources row, redirect to list.</step-5>
      <step-6>Pause/Resume: operator clicks action button, POST to `/admin/sites/:id/pause` or `/admin/sites/:id/resume`, UPDATE site_sources.status, redirect to list.</step-6>
      <step-7>Delete: operator clicks delete with confirmation, POST to `/admin/sites/:id/delete`, cascade DELETE (embeddings → chunks → pages → crawl_jobs → source), redirect to list.</step-7>
    </DF-ADMIN-SITES-CRUD>
    <DF-COUNTRY-BOOTSTRAP NAME="country settings startup flow">
      <step-1>M-SERVER bootstraps DB and runs schema migrations including country_settings table.</step-1>
      <step-2>M-DB-SITES-BOOTSTRAP seeds Japan country row if not exists: { country_code: "jp", status: "active", settings: { tg_chat_ids: [...] } }.</step-2>
      <step-3>M-COUNTRY-SETTINGS.buildCountryCache() loads all active countries into in-memory Map.</step-3>
      <step-4>M-TOOLS-CONTRACTS.buildToolSchemas(activeCountryCodes) creates tool schemas with dynamic country_code enum.</step-4>
      <step-5>CountryCache and ToolSchemas are passed to M-FASTMCP-RUNTIME for tool registration.</step-5>
    </DF-COUNTRY-BOOTSTRAP>

    <DF-COUNTRY-TOOL-ROUTING NAME="country-aware tool execution flow">
      <step-1>Tool call arrives at M-FASTMCP-RUNTIME with country_code in validated args.</step-1>
      <step-2>Execute handler resolves CountrySettings from countryCache.get(countryCode).</step-2>
      <step-3>For proxied tools: extracts tg_chat_ids from country.settings and passes CountryContext to M-TOOL-PROXY.</step-3>
      <step-4>For local tools: passes country_code to M-SITE-SOURCES or M-SITES-SEARCH for per-country data filtering.</step-4>
      <step-5>Usage tracking records (userId, toolName, countryCode) via M-USAGE-TRACKER.</step-5>
    </DF-COUNTRY-TOOL-ROUTING>
  </DataFlows>

  <MentalTests>
    <MT-1 NAME="portal onboarding e2e test">
      <step-1>Open shared landing URL as unauthenticated user.</step-1>
      <step-2>Register/login in portal and receive portal session.</step-2>
      <step-3>Open portal MCP guide and configure external agent.</step-3>
      <step-4>Execute one active MCP tool successfully.</step-4>
      <result>Pass when flow works without invite links and without manual admin permission grant.</result>
    </MT-1>

    <MT-2 NAME="triple auth isolation test">
      <step-1>Use portal session cookie against `/admin/*` and `/mcp`.</step-1>
      <step-2>Use admin cookie against `/portal/*` and `/mcp`.</step-2>
      <step-3>Use MCP OAuth token against `/admin/*` and `/portal/*`.</step-3>
      <result>Pass when each credential works only on its intended surface.</result>
    </MT-2>

    <MT-3 NAME="dropped tools surface test">
      <step-1>Enumerate MCP tools via runtime metadata.</step-1>
      <step-2>Verify `search_context`, `validate_google_map_link`, and `verify_tabelog` are absent.</step-2>
      <step-3>Verify unified cross-source search tool is absent and exactly seven approved tools remain callable (4 proxied + 3 local).</step-3>
      <result>Pass when dropped tools do not appear in runtime contracts, plan, or knowledge graph.</result>
    </MT-3>

    <MT-4 NAME="usage tracking e2e test">
      <step-1>Authenticated MCP client calls search_messages; execute handler extracts sub from context.session.idToken JWT.</step-1>
      <step-2>M-USAGE-TRACKER.recordToolCall(sub, "search_messages") performs UPSERT; usage_counters row created with call_count=1.</step-2>
      <step-3>Same client calls search_messages again; call_count increments to 2.</step-3>
      <step-4>Same user opens /portal/home; getUserStats(sub) returns {search_messages: 2, total: 2}; portal renders stats.</step-4>
      <result>Pass when MCP sub matches portal sub and counters increment correctly across calls.</result>
    </MT-4>

    <MT-5 NAME="usage tracking graceful degradation test">
      <step-1>MCP client authenticates but idToken is undefined and accessToken is opaque (not JWT).</step-1>
      <step-2>extractUserIdFromSession returns null; tool execution proceeds normally.</step-2>
      <step-3>recordToolCall is skipped; warning logged with tool name and reason.</step-3>
      <step-4>Tool result is returned to MCP client without error.</step-4>
      <result>Pass when tracking failure never blocks tool execution and warning is emitted.</result>
    </MT-5>

    <MT-6 NAME="usage tracker database failure resilience test">
      <step-1>PostgreSQL is temporarily unreachable during tool execution.</step-1>
      <step-2>recordToolCall catches database error internally and logs warning.</step-2>
      <step-3>Tool result is returned to MCP client without error.</step-3>
      <step-4>Portal home getUserStats also fails gracefully; page renders without stats or with fallback message.</step-4>
      <result>Pass when database failures are isolated from both tool execution and portal rendering.</result>
    </MT-6>
    <MT-7 NAME="get_site_sources happy path test">
      <step-1>Authenticated MCP client calls get_site_sources with empty args.</step-1>
      <step-2>canAccess passes; execute handler reads registry from `site_sources` table.</step-2>
      <step-3>Response contains description + tiers and source rows with domain/tier/status/crawl metadata.</step-3>
      <step-4>Usage counter increments for userId + "get_site_sources".</step-4>
      <result>Pass when response matches DB registry state and usage tracking fires.</result>
    </MT-7>

    <MT-8 NAME="get_site_sources unauthenticated test">
      <step-1>Unauthenticated MCP client calls get_site_sources.</step-1>
      <step-2>canAccess guard rejects — same behavior as proxied tools.</step-2>
      <result>Pass when access denied without reaching execute handler.</result>
    </MT-8>

    <MT-9 NAME="seven-tool surface completeness test">
      <step-1>Enumerate MCP tools via runtime metadata.</step-1>
      <step-2>Verify all seven tools present: search_messages, get_message_context, get_related_messages, list_sources, get_site_sources, search_sites, get_page_chunk.</step-2>
      <step-3>Verify first four proxy to upstream and last three execute locally.</step-3>
      <result>Pass when tool surface is exactly seven tools with correct execution paths.</result>
    </MT-9>

    <MT-10 NAME="curated-sites retrieval flow test">
      <step-1>Run worker crawl ingestion for at least one active source and persist chunks/embeddings.</step-1>
      <step-2>Call `search_sites` with a known query and confirm ranked attributed snippets are returned.</step-2>
      <step-3>Call `get_page_chunk` with returned chunk_id and verify bounded excerpt behavior.</step-3>
      <result>Pass when local site retrieval path works end-to-end without direct runtime crawl calls.</result>
    </MT-10>

    <MT-11 NAME="crawl-only policy test">
      <step-1>Attempt internal targeted recrawl for one URL.</step-1>
      <step-2>Verify ingestion uses Spider crawl path with strict cap, not scrape/manual endpoint.</step-2>
      <step-3>Verify no public MCP tool exposes manual refresh action.</step-3>
      <result>Pass when v1 crawl-only policy is enforced in both contracts and runtime behavior.</result>
    </MT-11>

    <MT-12 NAME="admin sites management CRUD test">
      <step-1>Authenticated admin opens `/admin/sites` and sees all seeded sources with page/chunk stats.</step-1>
      <step-2>Admin creates a new source via `/admin/sites/new` with valid fields; source appears in list.</step-2>
      <step-3>Admin edits source metadata via `/admin/sites/:id/edit`; changes are reflected.</step-3>
      <step-4>Admin pauses a source; worker skips it on next tick; admin resumes it; worker processes it again.</step-4>
      <step-5>Admin deletes a source; all related embeddings, chunks, pages, and crawl jobs are cascade-deleted.</step-5>
      <step-6>Admin submits invalid create form (e.g., tier=5, empty domain); validation errors are shown without side effects.</step-6>
      <result>Pass when all CRUD operations work correctly, cascade delete removes all related data, and validation prevents invalid entries.</result>
    </MT-12>

    <MT-13 NAME="admin sites HTMX progressive enhancement test">
      <step-1>Admin clicks "Sites Management" sidebar link with HTMX enabled.</step-1>
      <step-2>Server detects HX-Request header and returns content fragment only.</step-2>
      <step-3>HTMX swaps main content area without full page reload.</step-3>
      <step-4>Same page works with JavaScript disabled (full page navigation).</step-4>
      <result>Pass when page works with both HTMX fragment swap and full page load.</result>
    </MT-13>

    <MT-14 NAME="per-source cleanup pipeline e2e test">
      <step-1>Feed a noisy wrenjapan fixture (containing nav blocks, newsletter promos, image placeholders) to parseCrawlItem with sourceId="wrenjapan".</step-1>
      <step-2>Global cleanup (Layer A) removes data-URI images and social-share rows.</step-2>
      <step-3>wrenjapan adapter (Layer B) strips nav/menu blocks, newsletter promos, footer navigation.</step-3>
      <step-4>Quality gate (Layer C) computes clean_char_count; page passes MIN_CLEAN_CHARS threshold.</step-4>
      <step-5>parseCrawlItem returns { status: "accepted", page: ParsedPage } with clean article text and correct title.</step-5>
      <step-6>Feed same fixture without sourceId adapter match (e.g., sourceId="unknown"); only global cleanup applies, adapter is skipped.</step-6>
      <result>Pass when wrenjapan adapter removes source-specific noise, global cleanup handles universal patterns, and non-matching sources still get global cleanup only.</result>
    </MT-14>

    <MT-15 NAME="quality gate skip handling test">
      <step-1>Feed a near-empty crawl item (e.g., page with only nav/footer and no article body) to parseCrawlItem.</step-1>
      <step-2>After cleanup, clean_char_count falls below MIN_CLEAN_CHARS threshold.</step-2>
      <step-3>parseCrawlItem returns { status: "skipped", reason: "TOO_SHORT_AFTER_CLEANUP", source_id, url, metrics }.</step-3>
      <step-4>Ingestion orchestrator receives skip result, increments pages_skipped, logs info/warn with reason code.</step-4>
      <step-5>pages_skipped counter appears in IngestionResult; errors[] is NOT populated for this skip.</step-5>
      <step-6>Source ingestion continues processing remaining crawl items without aborting.</step-6>
      <result>Pass when quality-gated pages become structured non-fatal skips with reason codes, never pollute errors[], and don't interrupt ingestion flow.</result>
    </MT-15>

    <MT-16 NAME="all remaining source adapters fixture matrix test">
      <step-1>For each remaining source_id (insidekyoto, trulytokyo, kansai_odyssey, invisible_tourist, japan_unravelled, japan_guide, reddit_japantravel, navitime, jorudan, jreast, smart_ex), feed representative fixtures from `src/sites/parser/__fixtures__/&lt;source_id&gt;/raw/*.json` to parseCrawlItem.</step-1>
      <step-2>Assert source-specific adapter removes expected boilerplate markers while preserving domain-critical content tokens.</step-2>
      <step-3>Assert result remains `accepted` for known high-value pages and does not regress into false-positive skips.</step-3>
      <step-4>Compare pre/post clean_text lengths and ensure reductions are significant for noisy sources but bounded for low-noise official/reference pages.</step-4>
      <result>Pass when all remaining source adapters demonstrate deterministic noise reduction without content loss regressions.</result>
    </MT-16>

    <MT-17 NAME="multi-country search flow test">
      <step-1>User connects ONE MCP client to `/mcp` and authenticates via OAuth.</step-1>
      <step-2>User asks about ramen in Tokyo; AI sends search_messages({ query: "ramen Tokyo", country_code: "jp" }).</step-2>
      <step-3>M-FASTMCP-RUNTIME resolves "jp" from countryCache, gets tg_chat_ids from country_settings.</step-3>
      <step-4>M-TOOL-PROXY injects per-country chatIds into upstream call; results come back.</step-4>
      <step-5>M-USAGE-TRACKER.recordToolCall(userId, "search_messages", "jp") records country-specific usage.</step-5>
      <result>Pass when single MCP connection routes correctly to country-specific data sources and usage is tracked per-country.</result>
    </MT-17>

    <MT-18 NAME="unknown/inactive country rejection test">
      <step-1>Client calls search_messages({ query: "test", country_code: "zz" }).</step-1>
      <step-2>country_code "zz" is not in dynamic enum → Zod validation rejects before reaching execute handler.</step-2>
      <step-3>Even if enum bypass attempted, countryCache.get("zz") returns null → deterministic error result returned.</step-3>
      <result>Pass when invalid country codes are rejected at schema validation level and unknown countries produce deterministic errors.</result>
    </MT-18>

    <MT-19 NAME="add new country without code changes test">
      <step-1>Operator inserts country_settings row: { country_code: "it", status: "active", settings: { tg_chat_ids: [...] } }.</step-1>
      <step-2>Operator adds site_sources rows with country_code = "it".</step-2>
      <step-3>Server restart loads Italy into countryCache and rebuilds country_code enum.</step-3>
      <step-4>AI sends search_sites({ query: "pasta Rome", country_code: "it" }) → returns Italian site results.</step-4>
      <step-5>Portal destination list shows Italy as active destination.</step-5>
      <result>Pass when new country is fully functional with only DB inserts and server restart, no code changes.</result>
    </MT-19>
  </MentalTests>

  <ImplementationOrder>
    <Phase-1 name="ScopeAndContractsAlignment" status="done">
      <step-1>Synchronize requirements/technology/development-plan/knowledge-graph with portal-first scope and dropped tools list.</step-1>
      <step-2>Extend config contract with portal session requirements.</step-2>
      <justification>Architecture and contracts must reflect new product direction before module generation.</justification>
    </Phase-1>

    <Phase-2 name="PortalIdentityAndAuthCore" status="done">
      <step-1>Implement M-PORTAL-IDENTITY integration for social OAuth start/callback/provisioning endpoints.</step-1>
      <step-2>Implement M-PORTAL-AUTH session model for tester portal routes.</step-2>
      <step-3>Implement M-PORTAL-PROVISIONING automatic access grants.</step-3>
      <justification>Self-serve onboarding depends on identity integration and deterministic provisioning core logic.</justification>
    </Phase-2>

    <Phase-3 name="PortalAndLandingUI" status="done">
      <step-1>Implement simple landing with redirect CTA to `/portal`.</step-1>
      <step-2>Implement per-page portal routes (`/portal/register`, `/portal/login`, `/portal/auth/start`, `/portal/auth/callback`, `/portal/home`, `/portal/integrations/agent-setup`, `/portal/logout`).</step-2>
      <step-3>Implement Bun JSX SSR templates/components and compile Tailwind shared stylesheet asset.</step-3>
      <step-4>Add HTMX interactions for social-provider UI states and OAuth start actions; use Alpine.js only for optional local micro-interactions.</step-4>
      <justification>UI layer is meaningful only after auth/provisioning contracts are defined and must follow the approved lightweight SSR interaction model.</justification>
    </Phase-3>

    <Phase-4 name="RuntimeMountingAndE2EValidation" status="done">
      <step-1>Mount landing and portal routes in M-FASTMCP-RUNTIME alongside existing admin routes.</step-1>
      <step-2>Add integration/smoke tests for onboarding e2e and auth isolation.</step-2>
      <step-3>Confirm proxied tg-chat tool surface remains fixed to four active tools.</step-3>
      <justification>Runtime composition and tests validate real product objective: shareable tester onboarding to first MCP success.</justification>
    </Phase-4>

    <Phase-5 name="UsageTrackingAndPortalDisplay" status="done">
      <step-1>Add `DATABASE_URL` to M-CONFIG AppConfig type and loadConfig validation.</step-1>
      <step-2>Reactivate M-DB: wire createDb into M-SERVER bootstrap with fail-fast connectivity probe.</step-2>
      <step-3>Implement M-USAGE-TRACKER: Drizzle schema (usage_counters table), createUsageTracker factory, recordToolCall UPSERT, getUserStats query.</step-3>
      <step-4>Add extractUserIdFromSession helper in M-FASTMCP-RUNTIME: decode JWT payload from idToken/accessToken to extract sub claim.</step-4>
      <step-5>Update registerProxyTools execute handlers to accept context parameter, extract userId, and call usageTracker.recordToolCall after successful execution.</step-5>
      <step-6>Update M-PORTAL-UI handlePortalHomeRoute to query usageTracker.getUserStats and render per-tool call counts and total on portal home.</step-6>
      <step-7>Wire M-DB and M-USAGE-TRACKER into M-SERVER dependency bootstrap and pass usageTracker to runtime and portal handler deps.</step-7>
      <justification>Usage tracking requires database persistence (M-DB reactivation), a new data-layer module (M-USAGE-TRACKER), JWT identity extraction in tool execution (M-FASTMCP-RUNTIME), and portal display integration (M-PORTAL-UI). Steps are ordered by dependency: config first, then DB, then tracker, then runtime integration, then UI.</justification>
    </Phase-5>

    <Phase-6 name="CuratedSitesContractsAndSchema" status="done">
      <step-1>Extend M-CONFIG contract with `VOYAGE_API_KEY`, `SPIDER_API_KEY`, `PROXY_BASE_URL`, `PROXY_SECRET`.</step-1>
      <step-2>Add required site index schema/migrations (`site_sources`, `site_pages`, `site_chunks`, `site_chunk_embeddings`, `site_crawl_jobs`) with index_version support, execute `CREATE EXTENSION IF NOT EXISTS vector;`, and define `site_chunk_embeddings.embedding` as `vector(1024)` for `voyage-4` similarity search.</step-2>
      <step-3>Switch M-SITE-SOURCES runtime read path to `site_sources` table while keeping bootstrap constant as seed-only.</step-3>
      <step-4>Extend tool contracts for local `search_sites` and `get_page_chunk` schemas.</step-4>
      <justification>Contract and schema alignment must be complete before worker ingestion or local retrieval tools can be implemented safely.</justification>
    </Phase-6>

    <Phase-7 name="RuntimeSplitAndIngestionWorker" status="done">
      <step-1>Introduce dedicated worker entrypoint and startup wiring separate from API runtime.</step-1>
      <step-2>Implement Spider proxy crawl client and deterministic crawl-only ingestion orchestrator.</step-2>
      <step-3>Implement parser/chunker/embedding pipeline (`voyage-4`) and repository upserts for active index version.</step-3>
      <step-4>Implement scheduling cadence (5-min tick, tier-based intervals, weekly discovery pass) with jitter.</step-4>
      <justification>Worker isolation avoids API interference and enables predictable ingestion lifecycle and crawl budget enforcement.</justification>
    </Phase-7>

    <Phase-8 name="CuratedSitesLocalTools" status="done">
      <step-1>Register local `search_sites` and `get_page_chunk` tools in M-FASTMCP-RUNTIME (not in proxied allowlist).</step-1>
      <step-2>Implement M-SITES-SEARCH hybrid retrieval path and bounded chunk evidence response.</step-2>
      <step-3>Keep tool model explicit (`search_messages` + `search_sites`) and reject unified search abstraction.</step-3>
      <justification>This phase exposes user-facing curated retrieval while preserving clear tool boundaries and attribution guarantees.</justification>
    </Phase-8>

    <Phase-9 name="OpsHardening" status="done">
      <step-1>Add ingestion observability, retry/backoff policy, and provider outage fallback behavior.</step-1>
      <step-2>Add takedown/pause controls and strict bounded-output checks for evidence endpoints.</step-2>
      <step-3>Stabilize cost/performance via source budgets and cadence tuning.</step-3>
      <justification>Operational controls are required for production reliability, safety, and predictable operating cost.</justification>
    </Phase-9>

    <Phase-10 name="AdminSitesManagement" status="done">
      <step-1>Implement M-ADMIN-SITES data layer: fetchSitesPageData with per-source aggregated stats (page_count, chunk_count, embedding_count, last_crawl_at) and recent crawl jobs query.</step-1>
      <step-2>Implement M-ADMIN-SITES CRUD operations: handleCreateSource with Zod validation (CreateSourceInputSchema), handleUpdateSource (UpdateSourceInputSchema), handleDeleteSource with cascade delete, handleToggleSourceStatus.</step-2>
      <step-3>Implement M-ADMIN-SITES rendering: renderSitesContent (sources table with stats + action buttons + recent crawl jobs), renderSourceForm (create/edit form with validation error display).</step-3>
      <step-4>Update M-ADMIN-UI: extend ActiveTab union with "sites", add Sites Management sidebar link, add route handling for GET/POST /admin/sites, /admin/sites/new, /admin/sites/:id/edit, /admin/sites/:id/pause, /admin/sites/:id/resume, /admin/sites/:id/delete.</step-4>
      <step-5>Update M-SERVER: pass db handle into AdminUiDependencies so admin routes can query site tables.</step-5>
      <step-6>Add tests for M-ADMIN-SITES: data queries return correct aggregates, CRUD operations work, cascade delete removes all related records, Zod validation rejects invalid inputs, rendering produces expected HTML structure.</step-6>
      <justification>Admin sites management enables operators to add/edit/remove curated sources and monitor index health without direct database access.</justification>
    </Phase-10>

    <Phase-11 name="PerSourceCleanupAndQualityGate" status="done">
      <step-1>Define cleanup types and contracts: SkipReason enum, CleanupMetrics, SourceAdapter interface, ParserResult discriminated union, quality gate constants in `src/sites/parser/cleanup/types.ts`.</step-1>
      <step-2>Implement global cleanup rules (Layer A) in `src/sites/parser/cleanup/global.ts`: data-URI image removal, empty formatting fragment removal, social-share row stripping, whitespace normalization.</step-2>
      <step-3>Implement `wrenjapan` source adapter (Layer B) in `src/sites/parser/cleanup/adapters/wrenjapan.ts`: nav/menu block stripping, newsletter promo removal, image placeholder removal, footer navigation stripping.</step-3>
      <step-4>Implement quality gate (Layer C) in `src/sites/parser/cleanup/quality-gate.ts`: compute clean_char_count, apply MIN_CLEAN_CHARS threshold, return accept/skip decision with reason code.</step-4>
      <step-5>Implement cleanup pipeline orchestrator in `src/sites/parser/cleanup/index.ts`: wire Layer A → B → C, select source adapter by source_id, return CleanupResult.</step-5>
      <step-6>Refactor `parseCrawlItem` in `src/sites/parser/index.ts`: integrate cleanup pipeline, change return type to discriminated union (ParserResult), convert empty-content throw to structured skip, run title extraction on post-cleanup text with raw fallback.</step-6>
      <step-7>Update M-SITES-INGESTION `processSource`/`processPage`: handle ParserResult discriminated union, add `pages_skipped` counter to IngestionResult, remove empty-content error hack, log skips as info/warn with reason code and source metadata.</step-7>
      <step-8>Add parser unit tests: global cleanup rules against fixture snippets, wrenjapan adapter against `__fixtures__/wrenjapan/raw/` corpus, quality gate threshold decisions, discriminated union output for accepted and skipped pages.</step-8>
      <step-9>Add ingestion integration tests: verify skip handling does not populate errors[], verify pages_skipped counter increments, verify both scheduled and targeted recrawl paths handle parser skips.</step-9>
      <step-10>Validate with dry-run: compare before/after cleanup stats for wrenjapan fixtures, tune MIN_CLEAN_CHARS threshold based on fixture corpus analysis.</step-10>
      <step-11>Execute rollout: purge + recrawl for wrenjapan source to refresh index with cleanup pipeline output.</step-11>
      <justification>Per-source cleanup removes domain-specific crawl noise from indexed content, improving chunk quality and retrieval relevance for search_sites and get_page_chunk without changing MCP tool contracts. Discriminated union output and structured skip handling replaces the current error-based empty-content hack with clean non-fatal ingestion semantics.</justification>
    </Phase-11>

    <Phase-12 name="CleanupAdapterCoverageAllSources" status="done">
      <step-1>Add shared cleanup primitives in `src/sites/parser/cleanup/common.ts` and profile composers in `src/sites/parser/cleanup/profiles.ts` to avoid rule duplication across adapters.</step-1>
      <step-2>Implement Layer B adapters for all remaining source_ids using fixture-guided deterministic rules: insidekyoto, trulytokyo, kansai_odyssey, invisible_tourist, japan_unravelled, japan_guide, reddit_japantravel, navitime, jorudan, jreast, smart_ex.</step-2>
      <step-3>Implement `src/sites/parser/cleanup/registry.ts` with stable source_id-to-adapter mapping and exported SUPPORTED_CLEANUP_SOURCE_IDS list.</step-3>
      <step-4>Update cleanup pipeline orchestrator to consume registry module (keeping unknown-source fallback behavior unchanged).</step-4>
      <step-5>Expand parser test suite with fixture matrix coverage for every source adapter and preservation assertions for domain-critical tokens.</step-5>
      <step-6>Run phased rollout recrawl (editorial sources first, transit/community second, official/reference last) and monitor pages_skipped deltas before broad backfill.</step-6>
      <justification>Wren-only adapter coverage is insufficient for current curated registry breadth; full source coverage is required to reduce cross-domain crawl noise while keeping deterministic, auditable parser behavior.</justification>
    </Phase-12>

    <Phase-13 name="MultiTenantCountryArchitecture" status="done">
      <step-1>Add country_settings table to M-DB-SCHEMA and implement M-COUNTRY-SETTINGS repository (getCountrySettings, getCountriesByStatus, upsertCountrySettings, buildCountryCache). Test: CRUD operations, status filtering, cache construction.</step-1>
      <step-2>Add country_code column to site_sources table (M-DB-SCHEMA) with FK to country_settings and default 'jp'. Update M-SITES-INDEX-REPOSITORY searchHybrid to filter by country_code via site_sources join. Test: search filtering by country_code.</step-2>
      <step-3>Add country_code column to usage_counters table (M-DB-SCHEMA), extending composite PK to (user_id, tool_name, country_code). Update M-USAGE-TRACKER recordToolCall and getUserStats signatures. Test: per-country usage recording and stats filtering.</step-3>
      <step-4>Add country_code parameter to tool schemas in M-TOOLS-CONTRACTS: buildToolSchemas(activeCountryCodes) factory, country_code enum on search_messages, list_sources, get_site_sources, search_sites. Test: schema validation, enum membership, absence on get_page_chunk/get_message_context/get_related_messages.</step-4>
      <step-5>Update M-TOOL-PROXY executeTool to accept CountryContext { chatIds } parameter; buildUpstreamPayloadWithPolicy uses countryContext.chatIds instead of config.tgChatRag.chatIds. Test: upstream payload uses per-country chatIds.</step-5>
      <step-6>Update M-SITE-SOURCES getSiteSources to filter by country_code; replace frozen SITE_SOURCES_RESPONSE constant with DB-backed per-country query. Test: get_site_sources returns country-filtered sources, empty for unknown country.</step-6>
      <step-7>Wire country_code through M-FASTMCP-RUNTIME and M-SERVER: load country settings at startup, build countryCache, pass to runtime; resolve country context in tool execute handlers; update tool descriptions to generic multi-destination text; rename server to "travel-rag-mcp". Test: end-to-end tool call with country_code routing.</step-7>
      <step-8>Seed Japan country in M-DB-SITES-BOOTSTRAP: insert country_settings row { jp, active, tg_chat_ids from env }. Migrate existing data (site_sources/usage_counters default to 'jp'). Deprecate TG_CHAT_RAG_CHAT_IDS env var in M-CONFIG (optional, backwards compat). Test: server starts, search_messages with country_code=jp returns same results as before.</step-8>
      <step-9>Update M-PORTAL-UI for platform branding: replace "Japan Travel RAG" with TravelMind / PLATFORM_NAME, add destination list from country_settings, update MCP setup guide for single-endpoint multi-country usage. Add PLATFORM_NAME to M-CONFIG. Test: portal shows generic branding and destination list.</step-9>
      <justification>Single MCP endpoint with country_code parameter enables frictionless multi-destination UX (one OAuth flow, one connector), DB-only country provisioning, per-country analytics, and future per-country pricing — without per-URL routing complexity or multiple FastMCP instances.</justification>
    </Phase-13>
  </ImplementationOrder>

  <RiskAssessment>
    <risk-1 severity="high">Automatic provisioning can fail due to identity-provider API/permission misconfiguration, blocking onboarding despite valid registration.</risk-1>
    <risk-2 severity="high">Auth-boundary mistakes across `/mcp`, `/admin/*`, and `/portal/*` can create privilege crossover.</risk-2>
    <risk-3 severity="medium">Portal session secret/ttl misconfiguration can cause unstable login UX or accidental long-lived sessions.</risk-3>
    <risk-4 severity="medium">Social connector misconfiguration or provider outage can break both registration and login flows simultaneously.</risk-4>
    <risk-5 severity="medium">UI/contract drift between portal setup instructions and actual MCP OAuth requirements may increase tester setup failures.</risk-5>
    <risk-6 severity="low">Deprecated modules (M-MCP-AUTH-PROVIDER, M-MCP-AUTH-ADAPTER, M-API-KEY-REPOSITORY) remaining in repo can confuse future implementation unless graph linkage remains explicit.</risk-6>
    <risk-7 severity="medium">MCP OAuth session JWT (idToken/accessToken) may not always contain decodable sub claim depending on Logto configuration, causing silent tracking gaps.</risk-7>
    <risk-8 severity="low">PostgreSQL unavailability during tool execution causes usage tracking gaps but does not affect tool functionality due to fire-and-forget design.</risk-8>
    <risk-9 severity="medium">Spider provider outage or quota exhaustion can stall freshness for curated-site index.</risk-9>
    <risk-10 severity="medium">Improper crawl limits or missing jitter can cause bursty load and unstable ingestion latency.</risk-10>
    <risk-11 severity="medium">Index-version misconfiguration can mix old/new embeddings and degrade ranking consistency.</risk-11>
    <risk-12 severity="low">Bounded excerpt policy regressions in get_page_chunk can accidentally expose oversized content.</risk-12>
    <risk-13 severity="medium">Over-aggressive cleanup rules remove semantically useful content (article links, inline references) along with noise. Mitigation: conservative initial rules, fixture-based regression tests, per-source skip rate monitoring.</risk-13>
    <risk-14 severity="medium">Source website layout changes break source adapter pattern rules (wrenjapan nav structure changes). Mitigation: composable rules, raw-content fallback, adapter versioning strategy.</risk-14>
    <risk-15 severity="low">Quality gate false positives skip legitimate short-form pages (e.g., glossary entries, FAQ items). Mitigation: reason-coded logs enable threshold tuning with sampled page review.</risk-15>
    <risk-16 severity="medium">Adapter matrix growth across 12 sources can create maintenance drift and inconsistent cleanup semantics. Mitigation: shared common/profile modules, fixture matrix CI, and rollout by source group with monitored regressions.</risk-16>
    <risk-17 severity="medium">Adding country_code as required tool parameter is a breaking change for existing MCP clients that don't send it. Mitigation: transition period with optional country_code defaulting to "jp"; communicate schema change to users.</risk-17>
    <risk-18 severity="low">Country cache is loaded at startup only; newly added countries require server restart. Mitigation: acceptable for current scale; future hot-reload via cache invalidation endpoint if needed.</risk-18>
    <risk-19 severity="medium">Composite PK migration on usage_counters (adding country_code) requires careful handling of existing rows. Mitigation: column default 'jp' ensures existing data is preserved; migration is backwards-compatible.</risk-19>
  </RiskAssessment>

  <DeprecatedModules>
    <M-MCP-AUTH-PROVIDER NAME="McpAuthProvider" reason="Replaced by FastMCP OAuth Proxy integration for `/mcp`." />
    <M-MCP-AUTH-ADAPTER NAME="McpAuthAdapter" reason="No longer needed when FastMCP OAuth Proxy handles OAuth session flow." />
    <M-API-KEY-REPOSITORY NAME="ApiKeyRepository" reason="Out of active onboarding scope; admin remains ROOT_AUTH_TOKEN-only." />
  </DeprecatedModules>

  <DroppedModules>
    <M-SEARCH-CONTEXT-TOOL NAME="SearchContextTool" reason="Dropped from product plan by scope decision; not implemented in active roadmap." />
    <M-VERIFY-GMAP-TOOL NAME="ValidateGoogleMapLinkTool" reason="Dropped from product plan by scope decision; not implemented in active roadmap." />
    <M-VERIFY-TABELOG-TOOL NAME="VerifyTabelogTool" reason="Dropped from product plan by scope decision; not implemented in active roadmap." />
    <M-UNIFIED-SEARCH-TOOL NAME="UnifiedSearchTool" reason="Intentionally out of scope; product keeps explicit `search_messages` and `search_sites` tools." />
  </DroppedModules>
</DevelopmentPlan>
